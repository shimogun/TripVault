package com.example.trip_vault

import io.flutter.embedding.android.FlutterActivity

/**
 * TripVaultアプリのメインアクティビティ
 * Flutter Embedding V2を使用（マルチプラットフォーム対応）
 */
class MainActivity: FlutterActivity() {
    // 基本実装はFlutterActivityで提供されるため、
    // 特別な処理が必要な場合のみここに追加
}